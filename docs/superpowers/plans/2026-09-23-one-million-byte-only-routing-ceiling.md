# ReLAION-1M Byte-Only Routing Ceiling Plan

**Spec:** `docs/superpowers/specs/2026-09-23-one-million-byte-only-routing-ceiling-design.md`.

1. Write focused tests for complete ranked-order scan, byte skips,
   source-only seal equality, canonical samples and independent replay.
   Build the byte-only selector using the existing authenticated page
   centroids and exact group lengths; record GET count as diagnostic only.
2. Extend the one-shot selector worker to a fresh `byte` mode and output
   namespace. Keep the prior source seal SHA gates before query/truth,
   networkless evaluation, Spot interruption recovery, all-artifact
   readback and zero-swap/3-GiB resource gates. Run focused tests, Ruff
   and diff check, then commit/push the frozen source.
3. Archive with the source-commit sentinel, create-only upload/readback,
   check EC2 inventory and an empty attempt prefix, and run one Causality
   Spot controller. Observe terminal/infrastructure only. After closure,
   verify original controller exit, instance termination, every artifact
   and an independent aggregation of all 1,000 samples. Record the
   ceiling result and exact next ranking/layout gate in the ledger.
